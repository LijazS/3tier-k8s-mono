apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-redis
  namespace: 3tier-ns
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/redis
  persistentVolumeReclaimPolicy: Retain